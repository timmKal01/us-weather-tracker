{
    "title": "US Weather Tracker Input",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "locations": {
            "title": "Locations",
            "type": "array",
            "editor": "json",
            "description": "List of { \"label\": \"optional name\", \"latitude\": 30.2672, \"longitude\": -97.7431 }. US locations only (National Weather Service coverage).",
            "prefill": [
                { "label": "Austin, TX", "latitude": 30.2672, "longitude": -97.7431 }
            ],
            "default": [
                { "label": "Austin, TX", "latitude": 30.2672, "longitude": -97.7431 }
            ]
        },
        "forecastPeriods": {
            "title": "Forecast periods to include",
            "type": "integer",
            "editor": "number",
            "description": "How many forecast periods to return (each period is ~12 hours: day/night). Default 4 = next 2 days.",
            "default": 4,
            "minimum": 1,
            "maximum": 14
        }
    },
    "required": ["locations"]
}
